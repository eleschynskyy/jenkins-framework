apiVersion: v1
kind: Service
metadata:
  name: jenkins
  namespace: sandbox
spec:
  type: ClusterIP
  selector:
    app: jenkins
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
